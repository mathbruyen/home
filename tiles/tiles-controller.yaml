apiVersion: v1
kind: ReplicationController
metadata:
  name: tiles-v1
  labels:
    app: tiles
    version: v1
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: tiles
        version: v1
    spec:
      containers:
      - name: nginx
        image: nginx:1.11.1-alpine
        resources:
          requests:
            cpu: 50m
            memory: 100Mi
        ports:
        - containerPort: 80
        volumeMounts:
        - name: config
          mountPath: /etc/nginx/conf.d
          readOnly: true
        - name: tiles
          mountPath: /tiles
      volumes:
      - name: tiles
        emptyDir:
      - name: config
        configMap:
          name: tiles-config-v1
