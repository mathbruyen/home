apiVersion: v1
kind: ReplicationController
metadata:
  name: web-v17
  labels:
    app: web
    version: v17
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: web
        version: v17
    spec:
      containers:
      - name: nginx
        image: nginx:1.9.10
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
        ports:
        - containerPort: 80
        - containerPort: 443
        volumeMounts:
        - name: config
          mountPath: /etc/nginx/conf.d
          readOnly: true
        - name: certificates
          mountPath: /etc/certificates
          readOnly: true
      volumes:
      - name: certificates
        secret:
          secretName: webcertificates-v5
      - name: config
        secret:
          secretName: webconfiguration-v6
