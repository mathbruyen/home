apiVersion: v1
kind: ReplicationController
metadata:
  name: web-v11
  labels:
    app: web
    version: v11
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: web
        version: v11
    spec:
      containers:
      - name: nginx
        image: mathbruyen/web:0.0.13
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
        ports:
        - containerPort: 80
        - containerPort: 443
        volumeMounts:
        - name: certificates
          mountPath: /etc/certificates
          readOnly: true
      volumes:
      - name: certificates
        secret:
          secretName: webcertificates-v3
