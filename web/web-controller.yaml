apiVersion: v1
kind: ReplicationController
metadata:
  name: web-v23
  labels:
    app: web
    version: v23
spec:
  replicas: 2
  template:
    metadata:
      labels:
        app: web
        version: v23
    spec:
      containers:
      - name: nginx
        image: nginx:1.9.11
        resources:
          requests:
            cpu: 50m
            memory: 100Mi
        ports:
        - containerPort: 80
        - containerPort: 443
        volumeMounts:
        - name: config
          mountPath: /etc/nginx/conf.d
          readOnly: true
        - name: certificates
          mountPath: /etc/certificates
          readOnly: true
      volumes:
      - name: certificates
        secret:
          secretName: webcertificates-v8
      - name: config
        secret:
          secretName: webconfiguration-v10
