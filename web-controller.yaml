apiVersion: v1
kind: ReplicationController
metadata:
  name: web-v3
  labels:
    app: web
    version: v3
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: web
        version: v3
    spec:
      containers:
      - name: nginx
        image: mathbruyen/web:0.0.5
        resources:
          requests:
            cpu: 100m
            memory: 100Mi
        ports:
        - containerPort: 80
