apiVersion: v1
kind: ReplicationController
metadata:
  name: blog-v9
  labels:
    app: blog
    version: v9
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: blog
        version: v9
    spec:
      containers:
      - name: nginx
        image: mathbruyen/blog:20170207
        resources:
          requests:
            cpu: 10m
            memory: 10Mi
        ports:
        - containerPort: 80
        livenessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 60
          timeoutSeconds: 5
